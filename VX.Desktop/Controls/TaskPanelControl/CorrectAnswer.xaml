<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="CorrectAnswerTemplate" TargetType="{x:Type ListBoxItem}">
        <Grid x:Name="StealthButtonOuterGrid">
            <Border x:Name="ButtonBorder" CornerRadius="4,4,4,4" Margin="0,0,0,0" 
                    BorderBrush="Black" Opacity="1" BorderThickness="2">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#006701"/>
                        <GradientStop Offset="1" Color="#00ef01"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <Border x:Name="ButtonContent" Margin="5,5,5,5">
                <Label x:Name="ContentPresenterLabel" Foreground="Black" Opacity="1" FontWeight="Bold" Width="Auto" HorizontalAlignment="Center">
                    <ContentPresenter Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Center" FlowDirection="LeftToRight" OverridesDefaultStyle="True"></ContentPresenter>
                </Label>
            </Border>
        </Grid>
    </ControlTemplate>
</ResourceDictionary>
