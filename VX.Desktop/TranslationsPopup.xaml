<UserControl x:Class="VX.Desktop.TranslationsPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Loaded="UserControlLoaded">
    <UserControl.Resources>        
        <Style x:Key="questionPanel" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
        <Style x:Key="questionSpelling" TargetType="Label">
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="questionTranscription" BasedOn="{StaticResource questionSpelling}" TargetType="Label">
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
        <Style x:Key="answerItemBorder" TargetType="Border">            
            <Setter Property="BorderBrush" Value="#728D26"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>
        <Style x:Key="answerItemBorderNeutral" BasedOn="{StaticResource answerItemBorder}" TargetType="Border">
            <Setter Property="Background" Value="White"/>            
        </Style>
        <Style x:Key="answerItemBorderCorrect" BasedOn="{StaticResource answerItemBorder}" TargetType="Border">
            <Setter Property="Background" Value="#D4E59F"/>
        </Style>
        <Style x:Key="answerItemBorderWrong" BasedOn="{StaticResource answerItemBorder}" TargetType="Border">
            <Setter Property="Background" Value="Pink"/>
        </Style>
        <Style x:Key="HideAnswersStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="AnswerItem" Style="{StaticResource answerItemBorderNeutral}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="AnswerItem" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Color="#D4E59F"/>
                                            <GradientStop Color="White" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ShowAnswersStyleCorrect" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="AnswerItem" Style="{StaticResource answerItemBorderCorrect}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ShowAnswersStyleWrong" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="AnswerItem" Style="{StaticResource answerItemBorderWrong}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="answers" TargetType="ListBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
        <Style x:Key="answerItemText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#728D26"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style x:Key="titleBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="10,10,0,0"/>
            <Setter Property="Background" Value="#FF5A9AE0"/>
        </Style>
        <Style x:Key="contentBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="0,0,10,10"/>
            <Setter Property="Background" Value="#D4E59F"/>
        </Style>
    </UserControl.Resources>
    <Grid MinWidth="150">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Style="{StaticResource titleBorder}">
            <StackPanel Style="{StaticResource questionPanel}">
                <Label Name="questionSpelling" Style="{StaticResource questionSpelling}"/>
                <Label Style="{StaticResource questionSpelling}">[</Label>
                <Label Name="questionTranscription" Style="{StaticResource questionTranscription}" />
                <Label Style="{StaticResource questionSpelling}">]</Label>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Style="{StaticResource contentBorder}">
            <ListBox Name="answers" Style="{StaticResource answers}" SelectionChanged="AnswersSelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>                        
                        <TextBlock Style="{StaticResource answerItemText}" Text="{Binding Spelling}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
